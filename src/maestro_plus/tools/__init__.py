"""MCP tool implementations, grouped by concern.

Each module exposes plain annotated functions. Registration happens in
:mod:`maestro_plus.server` so the whole tool surface is auditable in one place —
which matters when the point of the project is a specific gap in someone else's
tool surface.
"""

__all__: list[str] = []
